import { MyColorizedButtonDirective } from './my-colorized-button.directive';

describe('MyColorizedButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new MyColorizedButtonDirective();
    expect(directive).toBeTruthy();
  });
});
