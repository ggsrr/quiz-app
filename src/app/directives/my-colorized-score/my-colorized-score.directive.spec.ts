import { MyColorizedScoreDirective } from './my-colorized-score.directive';

describe('MyColorizedScoreDirective', () => {
  it('should create an instance', () => {
    const directive = new MyColorizedScoreDirective();
    expect(directive).toBeTruthy();
  });
});
